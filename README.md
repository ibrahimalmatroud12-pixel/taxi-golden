# Golden Taxi Starter

This is a starter/demo implementation: manual trips, test START/STOP flow, acceptance counter, report, receipt preview, and login. The Chiron endpoints are placeholders until the official API contract and credentials are configured.

Render: Build `npm install`, Start `npm start`, Environment `DASHBOARD_PASSWORD`.
